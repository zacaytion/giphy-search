import * as React from "react";
import { hot } from "react-hot-loader";

const App: React.SFC = () => {
  return (
    <div>
      <p>React here!</p>
    </div>
  );
};

export default hot(module)(App);
